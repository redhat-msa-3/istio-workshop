diff --git a/recommendations/src/main/java/com/example/recommendations-v2/RecommendationsController.java b
index 8c6bf65..2de27cc 100644
--- a/recommendations/src/main/java/com/example/recommendations-v2/RecommendationsController.java
+++ b/recommendations/src/main/java/com/example/recommendations-v2/RecommendationsController.java
@@ -17,7 +17,7 @@ public class RecommendationsController {

         cnt ++;
         // hostname.substring(19) to remove "recommendations-v1-"
-        System.out.println("Big Red Dog v1 " + hostname.substring(19) + " " + cnt);
+        System.out.println("Big Red Dog v2 " + hostname.substring(19) + " " + cnt);

         /* begin timeout and/or circuit-breaker example
         try {
         @@ -35,7 +35,7 @@ public class RecommendationsController {
                      throw new ServiceUnavailableException("D'oh");
                  }
                  // */
-                 return "Clifford v1 " + hostname.substring(19) + " " + cnt;
+                 return "Clifford v2 " + hostname.substring(19) + " " + cnt;
              }

              @RequestMapping("/misbehave")
         @@ -51,4 +51,4 @@ class ServiceUnavailableException extends RuntimeException {
              public ServiceUnavailableException(String message) {
                super(message);
              }
-}
\ No newline at end of file
+}
